package com.leandrolcd.cameraonvif.exoplayer

import android.content.Context
import android.util.AttributeSet
import androidx.media3.ui.PlayerView

class CustomPlayerView @JvmOverloads constructor(
    context: Context, attrs: AttributeSet? = null, defStyleAttr: Int = 0
) : PlayerView(context, attrs, defStyleAttr) {

    // Puedes personalizar el diseño y comportamiento aquí
    // Por ejemplo, agregar controles personalizados, mostrar información adicional, etc.

}
